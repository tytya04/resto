# API Reference

## Обзор

Бот использует Telegram Bot API через фреймворк Telegraf. Все взаимодействия происходят через команды и callback-кнопки.

## Команды бота

### Общие команды

#### `/start`
Начало работы с ботом. Запускает процесс регистрации для новых пользователей.

**Доступно:** Всем пользователям

**Процесс:**
1. Выбор роли (ресторан/менеджер/закупщик)
2. Выбор ресторана (для роли "ресторан")
3. Ввод контактных данных
4. Ожидание подтверждения

#### `/profile`
Показывает профиль текущего пользователя.

**Доступно:** Зарегистрированным пользователям

**Ответ содержит:**
- Имя и username
- Роль в системе
- Привязанный ресторан
- Статус аккаунта

#### `/help`
Показывает справку по командам.

### Команды для ресторанов

#### `/restaurant_menu`
Главное меню для сотрудников ресторана.

**Доступно:** Пользователям с ролью `restaurant`

**Клавиатура:**
- 🆕 Новый заказ
- 📋 Мои заказы
- ⚙️ Настройки
- 🔙 Главное меню

#### `/create_order`
Создание нового заказа.

**Доступно:** Пользователям с ролью `restaurant` с привязкой к ресторану

**Процесс:**
1. Проверка незавершенных заказов
2. Создание нового или продолжение существующего
3. Переход в сцену добавления продуктов

#### `/my_orders`
Список заказов пользователя.

**Параметры:**
- Статус (опционально)
- Лимит (по умолчанию 20)

#### `/set_send_time`
Настройка времени автоматической отправки заказов.

**Доступно:** `restaurant`, `manager`

**Процесс:**
1. Выбор ресторана (для менеджеров)
2. Выбор времени из списка или ввод произвольного
3. Сохранение настроек

#### `/send_now`
Немедленная отправка всех черновиков заказов.

**Доступно:** `restaurant`, `manager`

### Команды для менеджеров

#### `/manager_menu`
Главное меню менеджера.

**Доступно:** Пользователям с ролью `manager`

**Клавиатура:**
- 📥 Новые заявки
- ⏳ В обработке
- ✅ Одобренные
- ❌ Отклоненные
- 📊 Статистика
- 💰 Рентабельность
- 📈 Обновить цены
- 📧 Email настройки
- ⚙️ Настройки

#### `/pending_orders`
Список новых заявок для обработки.

**Ответ:**
- Группировка по ресторанам
- Краткая информация о каждом заказе
- Команды для обработки

#### `/process_order_ID`
Начать обработку конкретного заказа.

**Параметры:**
- `ID` - идентификатор заказа

**Запускает сцену:** `process_order`

#### `/generate_torg12_ID`
Генерация документа ТОРГ-12 для заказа.

**Параметры:**
- `ID` - идентификатор заказа

**Требования:**
- Заказ должен быть в статусе `approved`

### Команды для закупщиков

#### `/procurement_menu`
Главное меню закупщика.

**Доступно:** Пользователям с ролью `buyer`

**Клавиатура:**
- 📋 Консолидированный список
- 🛒 Активные закупки
- ✅ Завершенные закупки
- 📊 Статистика закупок
- ⚙️ Настройки
- 🔙 Главное меню

#### `/consolidated_list`
Консолидированный список продуктов для закупки.

**Ответ:**
- Группировка по категориям
- Суммарное количество по всем ресторанам
- Средняя цена из истории
- Команды для начала закупки

#### `/purchase_PRODUCT_ID`
Начать закупку конкретного продукта.

**Параметры:**
- `PRODUCT_ID` - идентификатор консолидированного продукта

**Запускает сцену:** `purchase_product`

#### `/continue_purchase_ID`
Продолжить незавершенную закупку.

**Параметры:**
- `ID` - идентификатор закупки

### Команды для аналитики и отчетности

#### `/price_history [продукт]`
Показывает историю цен продукта.

**Доступно:** `manager`, `buyer`

**Параметры:**
- Название продукта

**Ответ:**
- История закупочных цен
- История цен продажи
- Рекомендованные цены
- Текущая маржинальность

#### `/profitability`
Отчет по рентабельности продуктов.

**Доступно:** `manager`

**Ответ:**
- Топ-10 прибыльных продуктов
- Общая статистика
- Выручка, себестоимость, маржа

#### `/update_prices`
Обновление цен в номенклатуре на основе закупок.

**Доступно:** `manager`, `admin`

**Процесс:**
1. Подтверждение операции
2. Обновление цен в Google Sheets
3. Перерасчет рекомендованных цен

#### `/order_analysis [номер]`
Анализ себестоимости конкретного заказа.

**Доступно:** `manager`

**Параметры:**
- Номер заказа

**Ответ:**
- Сравнение плановой и фактической себестоимости
- Отклонения по позициям
- Финансовые показатели

### Команды для документов

#### `/generate_torg12_ID`
Генерация ТОРГ-12 для заказа.

**Доступно:** `manager`, `buyer`

#### `/order_documents_ID`
Список документов для заказа.

#### `/documents_menu`
Меню управления документами.

### Команды для Email

#### `/email_settings`
Меню настроек email.

**Доступно:** `manager`

**Функциональность:**
- Настройка SMTP параметров
- Управление email адресами получателей
- Тестирование соединения
- Отправка тестовых писем

### Команды для администраторов

#### `/admin_panel`
Главная панель администратора.

**Доступно:** `admin`

**Меню:**
- 👥 Управление пользователями
- 🏢 Управление ресторанами
- ⚙️ Настройки системы
- 📊 Статистика
- 💾 Резервная копия БД
- 📋 Логи системы

#### `/users`
Список всех пользователей системы.

**Доступно:** `admin`

**Функции:**
- Просмотр с пагинацией (10 пользователей на страницу)
- Переход к управлению конкретным пользователем
- Фильтрация по ролям и статусу

#### `/restaurants`
Список всех ресторанов.

**Доступно:** `admin`

**Функции:**
- Просмотр всех ресторанов
- Статистика по каждому ресторану
- Добавление новых ресторанов

#### `/settings`
Системные настройки.

**Доступно:** `admin`

**Разделы:**
- Email настройки
- Расписание задач
- Системные уведомления

#### `/backup`
Создание резервной копии базы данных.

**Доступно:** `admin`

**Функции:**
- Создание PostgreSQL дампа
- Автоматическая архивация (gzip)
- Хранение последних 10 копий
- Информация о размере архива

#### `/stats`
Общая статистика системы.

**Доступно:** `admin`

**Показывает:**
- Количество пользователей по ролям
- Количество ресторанов
- Статистика заказов (всего, сегодня, за месяц)
- Статистика закупок
- Распределение заказов по статусам

#### `/user_ID`
Управление конкретным пользователем.

**Доступно:** `admin`

**Функции:**
- Блокировка/разблокировка
- Изменение роли
- Изменение привязки к ресторану
- Редактирование данных
- Удаление пользователя

#### `/restaurant_ID`
Управление конкретным рестораном.

**Доступно:** `admin`

**Функции:**
- Активация/деактивация
- Редактирование данных
- Управление пользователями ресторана
- Просмотр статистики

## Сцены (Scenes)

### `add_product`
Сцена добавления продукта в заказ.

**Шаги:**
1. Ввод названия продукта
2. Выбор из предложений
3. Ввод количества
4. Подтверждение

**Выход:** `/cancel` или завершение добавления

### `process_order`
Сцена обработки заказа менеджером.

**Шаги:**
1. Просмотр позиций
2. Редактирование цен
3. Добавление комментария
4. Подтверждение/отклонение

**Callback actions:**
- `change_price` - изменить цену
- `apply_suggested` - применить цену из номенклатуры
- `prev_item` / `next_item` - навигация
- `approve_order` - подтвердить заказ
- `reject_order` - отклонить заказ

### `purchase_product`
Сцена закупки продукта.

**Шаги:**
1. Показ информации о продукте и распределении по ресторанам
2. Ввод фактически закупленного количества
3. Ввод общей суммы закупки
4. Автоматический расчет цены за единицу
5. Показ распределения стоимости
6. Опциональное добавление примечания
7. Подтверждение закупки

**Callback actions:**
- `start_purchase` - начать закупку
- `quick_qty:{quantity}` - быстрый ввод количества
- `add_notes` - добавить примечание
- `skip_notes` - пропустить примечание
- `confirm_final` - подтвердить закупку
- `cancel_purchase` - отменить закупку

### `edit_smtp_scene`
Сцена настройки SMTP параметров.

**Шаги:**
1. Ввод SMTP хоста
2. Выбор/ввод порта
3. Ввод email для авторизации
4. Ввод пароля
5. Ввод email отправителя
6. Автоматическое тестирование соединения

**Callback actions:**
- `port_587` / `port_465` / `port_25` - быстрый выбор порта
- `use_auth_email` - использовать email авторизации как отправителя
- `cancel_smtp_setup` - отмена настройки

### `add_admin`
Сцена добавления нового администратора.

**Доступно:** `admin`

**Шаги:**
1. Ввод Telegram ID пользователя
2. Если пользователь существует - обновление роли
3. Если не существует - ввод данных (Имя Фамилия username)

**Выход:** `/cancel` или завершение добавления

### `add_restaurant`
Сцена создания нового ресторана.

**Доступно:** `admin`

**Шаги:**
1. Ввод названия ресторана
2. Ввод адреса (опционально)
3. Ввод телефона (опционально)
4. Ввод email (опционально)
5. Подтверждение данных

**Выход:** `/cancel` или завершение создания

### `edit_restaurant`
Сцена редактирования данных ресторана.

**Доступно:** `admin`

**Функции:**
- Изменение названия
- Изменение адреса
- Изменение телефона
- Изменение email

**Callback actions:**
- `edit_rest_name` - редактировать название
- `edit_rest_address` - редактировать адрес
- `edit_rest_phone` - редактировать телефон
- `edit_rest_email` - редактировать email
- `edit_rest_save` - сохранить изменения
- `edit_rest_cancel` - отменить редактирование

## Callback Queries

### Регистрация
- `reg_role:{role}` - выбор роли
- `reg_restaurant:{id}` - выбор ресторана
- `reg_new_restaurant` - запрос нового ресторана
- `reg_back_to_role` - вернуться к выбору роли

### Управление заказами
- `send_order` - отправить заказ
- `edit_order:{id}` - редактировать заказ
- `delete_order:{id}` - удалить заказ
- `cancel_order` - отменить заказ

### Поиск продуктов
- `select_category:{category}` - выбор категории
- `select_product:{index}` - выбор продукта
- `quick_qty:{product}:{quantity}:{unit}` - быстрый выбор количества
- `manual_qty:{product}` - ручной ввод количества
- `confirm:{product}:{quantity}:{unit}` - подтверждение добавления

### Обработка заказов
- `process_order:{id}` - начать обработку
- `change_price` - изменить цену позиции
- `apply_suggested` - применить предложенную цену
- `add_comment` - добавить комментарий
- `approve_order` - одобрить заказ
- `reject_order` - отклонить заказ
- `request_clarification` - запросить уточнения

### Документы
- `send_doc_email:{orderId}:{fileName}` - отправить на email
- `delete_doc:{fileName}` - удалить документ
- `quick_torg12:{orderId}` - быстрая генерация ТОРГ-12
- `cleanup_old_docs` - очистка старых документов

### Настройки
- `set_time_restaurant:{id}` - выбор ресторана для настройки
- `set_time:{restaurantId}:{time}` - установка времени
- `set_time_custom:{restaurantId}` - ввод произвольного времени
- `toggle_auto_send:{restaurantId}` - вкл/выкл автоотправку
- `send_now:{restaurantId}` - отправить сейчас

### Email настройки
- `edit_smtp` - редактировать SMTP настройки
- `edit_accountant_email` - изменить email бухгалтера
- `edit_manager_emails` - изменить email менеджеров
- `edit_notification_emails` - изменить дополнительные email
- `back_to_email_menu` - вернуться в меню email
- `cancel_test_email` - отменить тестовое письмо
- `cancel_email_edit` - отменить редактирование email

### Административные
- `admin_panel` - главное меню администратора
- `admin_users` - управление пользователями
- `admin_users_list` - список пользователей
- `admin_users_list_{page}` - страница списка пользователей
- `admin_users_search` - поиск пользователей
- `admin_users_pending` - заявки на регистрацию
- `admin_users_add_admin` - добавить администратора
- `admin_user_toggle_{id}` - блокировка/разблокировка пользователя
- `admin_user_role_{id}` - изменить роль пользователя
- `admin_user_restaurant_{id}` - изменить ресторан пользователя
- `admin_user_edit_{id}` - редактировать данные пользователя
- `admin_user_delete_{id}` - удалить пользователя
- `admin_restaurants` - управление ресторанами
- `admin_restaurants_list` - список ресторанов
- `admin_restaurants_add` - добавить ресторан
- `admin_restaurants_stats` - статистика по ресторанам
- `admin_restaurant_toggle_{id}` - активация/деактивация ресторана
- `admin_restaurant_edit_{id}` - редактировать ресторан
- `admin_restaurant_users_{id}` - пользователи ресторана
- `admin_restaurant_delete_{id}` - удалить ресторан
- `admin_settings` - настройки системы
- `admin_settings_email` - email настройки
- `admin_settings_schedule` - настройки расписания
- `admin_settings_notifications` - настройки уведомлений
- `admin_stats` - статистика системы
- `admin_detailed_analytics` - детальная аналитика
- `admin_backup` - создать резервную копию
- `admin_logs` - просмотр логов
- `admin_logs_clear` - очистить логи

## Модели данных

### User
```javascript
{
  id: INTEGER,
  telegram_id: STRING(50),
  username: STRING(100),
  first_name: STRING(100),
  last_name: STRING(100),
  phone: STRING(20),
  role: ENUM('restaurant', 'manager', 'buyer', 'admin'),
  restaurant_id: INTEGER,
  is_active: BOOLEAN,
  created_at: DATE,
  updated_at: DATE
}
```

### Order
```javascript
{
  id: INTEGER,
  order_number: STRING(20),
  restaurant_id: INTEGER,
  user_id: INTEGER,
  status: ENUM('draft', 'sent', 'processing', 'approved', 'rejected', 'completed'),
  total_amount: DECIMAL(10, 2),
  notes: TEXT,
  sent_at: DATE,
  approved_at: DATE,
  rejected_at: DATE,
  manager_comment: TEXT,
  created_at: DATE,
  updated_at: DATE
}
```

### OrderItem
```javascript
{
  id: INTEGER,
  order_id: INTEGER,
  product_name: STRING(300),
  quantity: DECIMAL(10, 3),
  unit: STRING(20),
  price: DECIMAL(10, 2),
  total: DECIMAL(10, 2),
  category: STRING(100),
  created_at: DATE,
  updated_at: DATE
}
```

### Purchase
```javascript
{
  id: INTEGER,
  consolidated_product_id: STRING(500),
  product_name: STRING(300),
  unit: STRING(20),
  total_quantity: DECIMAL(10, 3),
  purchased_quantity: DECIMAL(10, 3),
  total_price: DECIMAL(10, 2),
  unit_price: DECIMAL(10, 2),
  buyer_id: INTEGER,
  status: ENUM('pending', 'partial', 'completed', 'cancelled'),
  orders_data: JSON,
  notes: TEXT,
  purchase_date: DATE,
  created_at: DATE,
  updated_at: DATE
}
```

## Сервисы

### OrderService
```javascript
// Создание заказа
OrderService.createOrder(orderData)

// Получение заказа
OrderService.getOrderById(orderId)

// Обновление статуса
OrderService.updateOrderStatus(orderId, status, userId, reason)

// Добавление позиции
OrderService.addItemToOrder(orderId, itemData)

// Получение консолидированных заказов
OrderService.getConsolidatedOrders(dateFrom, dateTo, includeInProgress)

// Получение активных закупок
OrderService.getActivePurchases()

// Создание закупки из консолидированного списка
OrderService.createPurchaseFromConsolidated(consolidatedProduct, buyerId)

// Завершение закупки с распределением цен
OrderService.completePurchase(purchaseId, actualData)
```

### ProductMatcher
```javascript
// Поиск продуктов
ProductMatcher.suggestProducts(query, limit)

// Обучение синонимам
ProductMatcher.learnSynonym(userQuery, selectedProduct)

// Получение категорий
ProductMatcher.getCategories()
```

### DocumentGenerator
```javascript
// Генерация ТОРГ-12
DocumentGenerator.generateTorg12(orderId)

// Отправка на email (устаревший метод, использует EmailService)
DocumentGenerator.sendDocumentByEmail(filePath, email, subject, body)

// Получение документов заказа
DocumentGenerator.getGeneratedDocuments(orderNumber)
```

### EmailService
```javascript
// Инициализация сервиса
EmailService.initialize()

// Отправка email
EmailService.sendEmail(options)

// Отправка с шаблоном
EmailService.sendTemplatedEmail(templateName, data, to, subject)

// Отправка ТОРГ-12
EmailService.sendTorg12(order, pdfPath, recipientEmail)

// Отправка уведомления о новом заказе
EmailService.sendNewOrderNotification(order)

// Отправка ежедневного отчета
EmailService.sendDailyReport(orders, recipientEmail)

// Отправка уведомления об изменении цен
EmailService.sendPriceChangeNotification(changes, recipientEmail)

// Тестирование соединения
EmailService.testConnection()

// Обновление SMTP настроек
EmailService.updateSmtpSettings(settings)
```

### NotificationService
```javascript
// Отправка уведомления
NotificationService.sendNotification(telegramId, message, options)

// Уведомление о новом заказе
NotificationService.notifyNewOrder(order)

// Уведомление об обработке
NotificationService.notifyOrderProcessed(order, status)
```

## Middleware

### authMiddleware
Проверяет авторизацию пользователя.

```javascript
ctx.user // Объект пользователя из БД
```

### requireRole(role)
Проверяет роль пользователя.

```javascript
bot.command('manager_menu', requireRole('manager'), handler)
```

### requireRestaurant
Проверяет привязку к ресторану.

```javascript
bot.command('create_order', [requireRole('restaurant'), requireRestaurant], handler)
```

### requireAdmin
Проверяет права администратора.

```javascript
bot.command('admin_panel', requireAdmin, handler)
```

**Функциональность:**
- Проверка роли 'admin'
- Автоматический отказ в доступе для других ролей
- Защита всех административных команд

### loggerMiddleware
Расширенное логирование всех запросов с метриками производительности.

**Функциональность:**
- Автоматическое логирование всех команд
- Измерение времени выполнения
- Интеграция с MonitoringService
- Структурированные логи с контекстом

## Обработка ошибок

### ErrorHandler
Централизованная система обработки ошибок.

**Типы ошибок:**
- `ValidationError` - ошибки валидации
- `AuthenticationError` - ошибки авторизации
- `AuthorizationError` - недостаточно прав
- `NotFoundError` - ресурс не найден
- `ConflictError` - конфликт данных
- `DatabaseError` - ошибки БД
- `ExternalServiceError` - ошибки внешних сервисов

**Использование:**
```javascript
const { ValidationError } = require('./utils/errorHandler');

if (!isValid) {
  throw new ValidationError('Неверный формат данных');
}
```

### Глобальная обработка ошибок
```javascript
bot.catch((err, ctx) => {
  handleTelegramError(err, ctx);
});
```

## Мониторинг

### Health Check Endpoints
- `GET /health` - полная проверка системы
- `GET /liveness` - проверка жизнеспособности
- `GET /readiness` - готовность к работе
- `GET /metrics` - метрики Prometheus
- `GET /info` - информация о системе

### Метрики Prometheus
```
# Счетчики
bot_commands_total{command, status, user_role}
bot_errors_total{type, command}
bot_messages_processed_total{type}

# Гистограммы
bot_command_duration_seconds{command}
db_query_duration_seconds{operation}

# Gauges
bot_active_users
bot_orders_current{status}
bot_queue_size{queue_name}
```

## Конфигурация

### Переменные окружения
См. файл `.env.example`

### Настройки в БД
Таблица `settings`:
- `auto_send_time` - время автоотправки
- `auto_send_enabled` - включена ли автоотправка
- `smtp_host` - хост SMTP сервера
- `smtp_port` - порт SMTP
- `smtp_secure` - использовать SSL/TLS
- `smtp_user` - пользователь для авторизации
- `smtp_pass` - пароль SMTP
- `smtp_from` - email отправителя
- `accountant_email` - email бухгалтера для ТОРГ-12
- `manager_emails` - email менеджеров (через запятую)
- `notification_emails` - дополнительные email для уведомлений

## Ошибки

### Коды ошибок
- `UNAUTHORIZED` - пользователь не авторизован
- `FORBIDDEN` - недостаточно прав
- `NOT_FOUND` - ресурс не найден
- `VALIDATION_ERROR` - ошибка валидации
- `INTERNAL_ERROR` - внутренняя ошибка

### Обработка ошибок
```javascript
bot.catch((err, ctx) => {
  logger.error(`Error for ${ctx.updateType}`, err);
  ctx.reply('Произошла ошибка. Пожалуйста, попробуйте позже.');
});
```